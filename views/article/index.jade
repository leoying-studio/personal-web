extends ../layout
block content
 section
  div.article-container
   div.article-wrapper.clearfix
    include ./verticle-item.jade
   div.article-paging-wrapper
     div.paging#articlePaging(navId="#{params.navId}" categoryId="#{params.categoryId}" currentPage="#{params.currentPage}" total="#{total}")
